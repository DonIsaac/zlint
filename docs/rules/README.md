# Rule docs

Rule documentation is generated from `src/linter/rules/*.zig` doc comments into
`apps/site/docs/rules/*.mdx` by `just codegen`.

Read the maintained in-repo rule docs in [`apps/site/docs/rules`](../../apps/site/docs/rules/).
